@model Army_Constractor.Models.Unit

@{
    ViewBag.Title = "Details";
}

<table class="tableElement" border="1">
    <tr>
        <td colspan="3" style="font-size:x-large">@Model.Armor.ArmorName</td>
    </tr>
    <tr>
        <td>Пгл</td>
        <td>Снж</td>
        <td rowspan="8" style="font-size:xx-large" id="price">
            @Model.Armor.Price
        </td>
    </tr>
    <tr>
        <td>
            @Model.Armor.ArmorAbsorb
        </td>
        <td>
            @Model.Armor.ArmorMoveDecrease
        </td>
    </tr>
    <tr>
        <td align="right" colspan="2" style="font-size:large">Стоимость:</td>
    </tr>
    <tr>
        <td colspan="2">
            @*@Html.ActionLink("Редактировать", "Edit", new { id = Model.Armor.ArmorID }) |
            @Html.ActionLink("Подробнее", "Details", new { id = Model.Armor.ArmorID }) |
            @Html.ActionLink("Удалить", "Delete", new { id = Model.Armor.ArmorID })*@
        </td>
    </tr>
</table>
