@model IEnumerable<Army_Constractor.Models.Army>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ArmyName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UnitsInArmyID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ArmyName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UnitsInArmyID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ArmyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ArmyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ArmyID })
        </td>
    </tr>
}

</table>
